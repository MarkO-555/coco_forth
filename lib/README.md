# `lib/` — Shared Forth Libraries

> *Auto-generated by `tools/gen-lib-readme.py`. Do not hand-edit — update each file's `.fs` header and re-run the generator (`make lib-readme` or `python3 tools/gen-lib-readme.py`).*

Each table lists the public words from one file. Words whose names begin with `_` are treated as private and omitted.

---

## Files

- [`beam.fs`](#beamfs)
- [`bye.fs`](#byefs)
- [`cg-text.fs`](#cg-textfs)
- [`dac.fs`](#dacfs)
- [`datawrite.fs`](#datawritefs)
- [`font-art.fs`](#font-artfs)
- [`font5x7.fs`](#font5x7fs)
- [`fujinet.fs`](#fujinetfs)
- [`keyboard.fs`](#keyboardfs)
- [`print.fs`](#printfs)
- [`prox.fs`](#proxfs)
- [`rg-pixel.fs`](#rg-pixelfs)
- [`rg-text.fs`](#rg-textfs)
- [`rng.fs`](#rngfs)
- [`screen.fs`](#screenfs)
- [`sg6-text.fs`](#sg6-textfs)
- [`sound.fs`](#soundfs)
- [`sprite.fs`](#spritefs)
- [`trig.fs`](#trigfs)
- [`vdg.fs`](#vdgfs)

---

## `beam.fs`

Beam pixel-save/restore system for artifact-free rendering

**Provides:** beam-trace, beam-draw-slice (kernel primitives) beam-restore-slice (CODE word, defined here)

**Requires:** kernel (VAR_BEAM_BUF, VAR_BEAM_VRAM, VAR_BEAM_CNT, VAR_LINE_*, VAR_RGVRAM), rg-pixel.fs

| Word | Stack | Kind |
|------|-------|------|
| `beam-restore-slice` | `( buf start count -- )` | CODE |

---

## `bye.fs`

Clean program exit

**Provides:** bye, basic-cold, exit-basic

**Requires:** vdg.fs (reset-text), screen.fs (cls-black), kernel halt

| Word | Stack | Kind |
|------|-------|------|
| `bye` | `( -- )` | colon |
| `basic-cold` | `( -- )` | CODE |
| `exit-basic` | `( -- )` | colon |

---

## `cg-text.fs`

CG mode text rendering (2 bits/pixel)

**Provides:** init-expand, expand-hi, expand-lo, cg-char

**Requires:** glyph-addr (from font5x7.fs), cv, cb (from rg-text.fs), tp, tb (from datawrite.fs), kernel primitives RSHIFT, AND, IF, ELSE, THEN, *, +, @, !, C@, C!, SWAP, OVER, DROP, DO, LOOP, I

| Word | Stack | Kind |
|------|-------|------|
| `init-expand` | `( -- )` | colon |
| `expand-hi` | `( byte -- cg-byte )` | colon |
| `expand-lo` | `( byte -- cg-byte )` | colon |
| `cg-char` | `( char cx cy -- )` | colon |

---

## `dac.fs`

Joystick reading via DAC successive approximation

**Provides:** joy-sel-rx, joy-sel-ry, joy-sel-lx, joy-sel-ly, joy-bit, joy-sample, joy-x, joy-y, joy-fire?, joy-fire-l?

**Requires:** kernel primitives C@, C!, AND, OR, 2DUP, DROP, LSHIFT, KBD-SCAN

| Word | Stack | Kind |
|------|-------|------|
| `joy-sel-rx` | `( -- )` | colon |
| `joy-sel-ry` | `( -- )` | colon |
| `joy-sel-lx` | `( -- )` | colon |
| `joy-sel-ly` | `( -- )` | colon |
| `joy-bit` | `( result bit -- result' )` | colon |
| `joy-sample` | `( -- 0..63 )` | colon |
| `joy-x` | `( -- 0..63 )` | colon |
| `joy-y` | `( -- 0..63 )` | colon |
| `joy-fire?` | `( -- flag )` | colon |
| `joy-fire-l?` | `( -- flag )` | colon |

---

## `datawrite.fs`

Data table construction helpers

**Provides:** tp (variable), tb, tw

**Requires:** kernel primitives C!, !, @, +

| Word | Stack | Kind |
|------|-------|------|
| `tp` |  | var |
| `tb` | `( byte -- )` | colon |
| `tw` | `( word -- )` | colon |

---

## `font-art.fs`

Artifact-safe font for RG6 NTSC display

**Provides:** init-font, glyph-addr

**Requires:** kernel primitives CMOVE, font-data fc.py-injected constant: font-base

| Word | Stack | Kind |
|------|-------|------|
| `glyph-addr` | `( char -- addr )` | colon |
| `init-font` | `( -- )` | colon |

---

## `font5x7.fs`

5x7 bitmap font for VDG graphics modes

**Provides:** init-font, glyph-addr

**Requires:** kernel primitives C!, +, -, *, <, AND, IF/ELSE/THEN, DO/LOOP fc.py-injected constant: font-base

| Word | Stack | Kind |
|------|-------|------|
| `glyph-addr` | `( char -- addr )` | colon |
| `init-font` | `( -- )` | colon |

---

## `fujinet.fs`

FujiNet device support for the CoCo

**Provides:** dw-write, dw-read, FN-OP-FUJI, FN-CMD-READY, FN-CMD-TIME (protocol constants), fn-cmd (buffer variable), fn-ping, fn-ready, fn-ready/N, fn-time, fn-time/N

**Requires:** HDB-DOS cart at $D93F (DWRead) and $D941 (DWWrite), kernel CODE infrastructure (no Forth-level deps)

| Word | Stack | Kind |
|------|-------|------|
| `dw-write` | `( buf len -- )` | CODE |
| `dw-read` | `( buf len -- ok )` | CODE |
| `FN-OP-FUJI` |  | const |
| `FN-CMD-READY` |  | const |
| `FN-CMD-TIME` |  | const |
| `fn-cmd` |  | var |
| `fn-ping` | `( -- ok )` | colon |
| `fn-ready` | `( -- )` | colon |
| `fn-ready/N` | `( n -- ok )` | colon |
| `fn-time` | `( buf -- )` | colon |
| `fn-time/N` | `( buf n -- ok )` | colon |

---

## `keyboard.fs`

CoCo keyboard matrix constants and utilities

**Provides:** KB-C0..KB-C7 (column strobe masks), KB-R0..KB-R7 (row bit masks), KEY-HELD?

**Requires:** kernel primitives KBD-SCAN, AND, SWAP

| Word | Stack | Kind |
|------|-------|------|
| `KB-C0` |  | const |
| `KB-C1` |  | const |
| `KB-C2` |  | const |
| `KB-C3` |  | const |
| `KB-C4` |  | const |
| `KB-C5` |  | const |
| `KB-C6` |  | const |
| `KB-C7` |  | const |
| `KB-R0` |  | const |
| `KB-R1` |  | const |
| `KB-R2` |  | const |
| `KB-R3` |  | const |
| `KB-R4` |  | const |
| `KB-R5` |  | const |
| `KB-R6` |  | const |
| `KB-R7` |  | const |
| `KEY-HELD?` | `( col_mask row_bit -- flag )` | colon |

---

## `print.fs`

number printing utilities

**Provides:** u., .

**Requires:** kernel primitives NEGATE, ?DUP, DUP, IF, /MOD, +, EMIT, 0, <

| Word | Stack | Kind |
|------|-------|------|
| `u.` | `( u -- )` | colon |
| `.` | `( n -- )` | colon |

---

## `prox.fs`

Spatial proximity query (Manhattan-distance bitmask)

**Provides:** prox-scan

**Requires:** kernel CODE infrastructure

| Word | Stack | Kind |
|------|-------|------|
| `prox-scan` | `( cx cy radius array count -- bitmask )` | CODE |

---

## `rg-pixel.fs`

RG6 artifact-color pixel primitives

**Provides:** rv (VRAM base variable), rg-init, rg-init-at, rg-pcls, rg-pset (kernel), rg-pget, rg-addr, rg-hline, rg-line (kernel)

**Requires:** kernel primitives, vdg.fs (set-sam-v, set-sam-f, set-pia)

| Word | Stack | Kind |
|------|-------|------|
| `rv` |  | var |
| `rg-init-at` | `( base -- )` | colon |
| `rg-init` | `( -- )` | colon |
| `rg-pcls` | `( -- )` | colon |
| `rg-addr` | `( x y -- )` | colon |
| `rg-pget` | `( x y -- raw )` | colon |
| `rg-hline` | `( x1 x2 y color -- )` | colon |

---

## `rg-text.fs`

RG mode text rendering (1 bit/pixel)

**Provides:** cv (variable), cb (variable), rg-char, rg-type

**Requires:** glyph-addr (from font5x7.fs)

| Word | Stack | Kind |
|------|-------|------|
| `cv` |  | var |
| `cb` |  | var |
| `rg-char` | `( char cx cy -- )` | colon |
| `rg-type` | `( addr len cx cy -- )` | colon |

---

## `rng.fs`

compatibility shim

**Provides:** seed

**Requires:** kvar-seed (kernel-exposed VAR_SEED address)

| Word | Stack | Kind |
|------|-------|------|
| `seed` | `( -- addr )` | colon |

---

## `screen.fs`

VDG screen utilities and vsync

**Provides:** vsync, wait-past-row, count-blanking, cls-black, cls-green

**Requires:** kernel primitives (vsync, wait-past-row, count-blanking are now kernel CODE words; cls-black/cls-green are Forth)

| Word | Stack | Kind |
|------|-------|------|
| `cls-black` | `( -- )` | colon |
| `cls-green` | `( -- )` | colon |

---

## `sg6-text.fs`

SG6 mode text rendering (block-pixel font)

**Provides:** sg6-char, sg6-row

**Requires:** glyph-addr (from font5x7.fs), cv (from rg-text.fs), kernel primitives AND, IF, ELSE, THEN, DUP, +, @, !, C@, C!, DO, LOOP, I, DROP

| Word | Stack | Kind |
|------|-------|------|
| `sg6-row` | `( font-byte -- )` | colon |
| `sg6-char` | `( char cx cy -- )` | colon |

---

## `sound.fs`

DAC sound library for the CoCo

**Provides:** snd-init, snd-tone, snd-saw, snd-tri, snd-sin, snd-noise, snd-pause, snd-beep, snd-zap, snd-boom, snd-chirp, snd-dock, snd-hit

**Requires:** kernel primitives rng + kvar-seed (for snd-noise), lib/trig.fs init-sin populating trig-base (for snd-sin)

| Word | Stack | Kind |
|------|-------|------|
| `snd-init` | `( -- )` | CODE |
| `snd-tone` | `( pitch duration -- )` | CODE |
| `snd-saw` | `( step pitch duration -- )` | CODE |
| `snd-tri` | `( pitch duration -- )` | colon |
| `snd-sin` | `( pitch duration -- )` | CODE |
| `snd-noise` | `( delay duration -- )` | colon |
| `snd-beep` | `( -- )` | colon |
| `snd-zap` | `( -- )` | colon |
| `snd-boom` | `( -- )` | colon |
| `snd-pause` | `( count -- )` | colon |
| `snd-chirp` | `( -- )` | colon |
| `snd-dock` | `( -- )` | colon |
| `snd-hit` | `( -- )` | colon |

---

## `sprite.fs`

Software sprite library for RG6 artifact-color mode

**Provides:** spr-w, spr-h (field accessors) spr-draw, spr-erase-box (kernel primitives)

| Word | Stack | Kind |
|------|-------|------|
| `spr-w` | `( addr -- width )` | colon |
| `spr-h` | `( addr -- height )` | colon |

---

## `trig.fs`

Sine/cosine lookup table for angle-based operations

**Provides:** init-sin, sin, cos, angle-dx, angle-dy

**Requires:** kernel primitives C@, *, +, -, /MOD, NEGATE, RSHIFT, CMOVE, sin-data, DUP, DROP, SWAP, OVER, @, !, <, >, 0=, IF, ELSE, THEN

| Word | Stack | Kind |
|------|-------|------|
| `init-sin` | `( -- )` | colon |
| `sin` | `( angle -- value )` | colon |
| `cos` | `( angle -- value )` | colon |
| `angle-dx` | `( angle length -- dx )` | colon |
| `angle-dy` | `( angle length -- dy )` | colon |

---

## `vdg.fs`

VDG/SAM display mode switching library

**Provides:** set-sam-v, set-sam-f, set-pia, reset-text

**Requires:** kernel primitives C@, C!, AND, OR, RSHIFT, DUP, DROP, IF/ELSE/THEN

| Word | Stack | Kind |
|------|-------|------|
| `set-sam-v` | `( v -- )` | colon |
| `set-sam-f` | `( offset -- )` | colon |
| `set-pia` | `( bits -- )` | colon |
| `reset-text` | `( -- )` | colon |

---
